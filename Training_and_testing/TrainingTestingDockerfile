FROM python:3.11-slim
FROM model-lib__v2

WORKDIR /app

COPY . .

RUN pip3 install -r requirements.txt

CMD ["streamlit", "run", "Training_and_testing_streamlit.py", "--server.port=8501", "--server.address=0.0.0.0"]

